{
  "name": "techsHQ.github.io",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://techshq.github.io/",
  "devDependencies": {
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run clean && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "clean": "rm -rf build static",
    "deploy": "npm run build && mv -f build/* . && git add . && git commit"
  }
}
